{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the LOMAH window is allowed to ask the Rust side for.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
